export default {
  // Get Users
  //   async loadUsers(context, [payload, page]) {
  //     try {
  //       const response = await POSTCallHelper("users", payload, page);
  //       console.log("RESONSE: ", response.data.users);
  //       context.commit("setUsers", response.data.users);
  //     } catch (e) {
  //       handleError(e.response.data);
  //     }
  //   },
};
